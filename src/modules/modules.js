// export fullname
export function fullname(firstname, lastname) {
    return firstname + ' ' + lastname+ '<br />';
}

// export initials
export function initials(firstname, lastname) {
  return firstname[0] + '. ' + lastname[0] + '. <br />';
}

// export variables
export let letters = 'ABC';
export let cijfers = 123;
